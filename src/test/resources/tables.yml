## YAML Template.
---
tables:
    - name: users_meta
      columns:
        - id
        - user_id
        - meta_key
        - meta_value
      primary_key: id
      relation:
          column: user_id
          table: users
          parent_column: id
    - name: users
      columns:
        - id
        - username
        - email
        - address
        - phone
      primary_key: id
